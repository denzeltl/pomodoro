import React from 'react';
import Display from './Display';
import Button from './Button';

const Main = () => {
    return (
        <main className="main">
            <Display />
            <Button />
        </main>
    );
};

export default Main;
